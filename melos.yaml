name: quran

packages:
  - packages/**
  - app/**

command:
  version:
    branch: master
    releaseUrl: true

scripts:
  format:
    run: melos exec dart format . --fix
    description: Run `dart format` for all packages.